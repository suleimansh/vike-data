// Per-page override: this route uses the `sidebar` shell instead of the app-default `topbar`.
// Same logo / nav / footer / userMenu slots — only the arrangement changes.
export default {
  layout: 'sidebar',
}
