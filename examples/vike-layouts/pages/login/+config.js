// The public `centered` shell — logo over a single centered card, no app nav or user menu.
// This is what an auth / marketing page uses; the app-nav slots are dropped even though the
// app set them, because `centered` doesn't declare them.
export default {
  layout: 'centered',
}
