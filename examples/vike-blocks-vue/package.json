{
  "name": "app-vike-blocks-vue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal vike-vue example rendering the vike-blocks catalog: the Vue twin of app-vike-blocks. Focused on the INTERACTIVE blocks (accordion / dialog / dropdown) so the Vue block renderers can be exercised in a browser — there was no Vue counterpart to the React gallery.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vike": "^0.4.0",
    "vike-vue": "^0.9.11",
    "vike-blocks": "workspace:*",
    "vite": "^7.1.0",
    "@vitejs/plugin-vue": "^5.0.0"
  }
}
