// /docs/:slug — every doc page except the index (installation, setup, routing, …). One dynamic route
// renders them all from the content registry; DocRoute reads the current path to pick the page.
export { default } from '../../../DocRoute.jsx'
