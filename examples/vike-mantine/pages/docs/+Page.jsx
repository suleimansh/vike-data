// /docs (the index / Introduction page). The docs site is data-driven — see DocRoute + docs-content.
// It fills the page (no artificial frame): the shell's own sticky navbar is the header, and the app
// chrome header is hidden on /docs routes (see pages/Layout.jsx) so it reads as a real doc site.
export { default } from '../../DocRoute.jsx'
