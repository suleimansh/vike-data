{
  "name": "app-vike-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal vike-auth example (React, memory adapter, zero DB): install vike-auth/react to get magic-link login, the extension's own /login + /account pages, useUser(), and one guarded page. No themes, no admin, no data layer.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@universal-orm/core": "workspace:*",
    "@universal-orm/memory": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vike": "^0.4.0",
    "vike-react": "^0.6.0",
    "vike-auth": "workspace:*",
    "vike-i18n": "workspace:*",
    "vike-layouts": "workspace:*",
    "vike-themes": "workspace:*",
    "vike-toolbar": "workspace:*",
    "vite": "^7.1.0"
  }
}
