---
'vike-mail': patch
---

The dev console/outbox transport now logs the message's text body, so the magic link (and any notification mail) is actually visible in the server console, as the sign-in UI promises.
